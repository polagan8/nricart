# NRICart frontend

Static storefront organized for maintainability.

- `index.html` — semantic page structure and metadata
- `css/styles.css` — design system, components, responsive rules
- `js/app.js` — Supabase product loading, cart behavior, and motion
- `assets/` — local imagery

Deploy the repository root as a static site on Vercel.
